# Local Expo Go only. Use the development machine's reachable URL when testing
# on a physical device. Never commit .env or .env.local.
EXPO_PUBLIC_API_BASE_URL=

# Every EAS preview APK must receive an approved HTTPS value from the EAS
# preview environment. This value is public application configuration, not a secret.
