# Togt — Handoff for Next Session (2026-05-27)

**Branch HEAD:** `66c5834` on `feat/fly-production-deploy` (Mac mini, NOT pushed yet). `main` is at `f53003f` (unchanged from 2026-05-09). Working tree clean on Mac.

**Tests:** **198/198 unit + 7/7 smoke** (last green run at start of session). No regressions touched in this session — all new work is on stacked branches.

**Backend live state:** still on Mac mini launchd `com.togt.backend` port 3002. Production deploy in flight but not yet live.

---

## What shipped this session

Five distinct chunks of work landed across two repos. Everything is on stacked PRs awaiting Damian's review; nothing has been merged.

### 1. Audit log primitive (PR DamianOost/Togt#1)

Branch: `feat/audit-log-and-smokes` — 9 commits.

- Migration `015_audit_log.sql` with 7 indexes (partial on `error_code`, GIN on `metadata`, agent timeline, user timeline, resource history, action analytics)
- `services/auditLog.js` — `recordAudit` (pool-direct writes so failures still leave a trail) + `recordAuditFireAndForget` (middleware-safe wrapper)
- `middleware/auditLog.js` — captures req+res on `res.on('finish')`, resolves actor from `req.apiKey` (preferred) or `req.user`, skips under `NODE_ENV=test` (deadlock fix)
- `audit_log_query` MCP tool — paginated, scope-gated (own trail by default, admin scope unlocks cross-actor)
- Cursor precision fix — `occurred_at::text` round-trip preserves Postgres microseconds (pg-node `Date` is ms-only)
- 3 warm-up cleanups from the 2026-05-09 audit: dead `errorHandler.js` deleted, sweeper `last_success_at` + `isFresh()` + `/health/deep` wire-up, `emitEvent` chunking-loop coverage

**Test delta:** 165 → 198 (+33).

### 2. Smoke test harness (PR DamianOost/Togt#2, stacked on #1)

Branch: `feat/smoke-tests` — 3 commits.

- `tests/smoke/jest.smoke.json.js` (Jest config under tests/ so default `npm test` doesn't auto-discover it)
- `tests/smoke/setupEnv.js` — flips `NODE_ENV=smoke` before any require so the audit middleware actually fires
- `tests/smoke/harness.js` — boots app + db + dispatcher (workers stay off unless `.tick()`d), exposes `registerUser` / `mintApiKey` / `createSubscription` / `startWebhookSink` / `drainDispatcher`
- 3 smoke files / 7 tests: auth full loop (1), webhook delivery end-to-end with HMAC verification (2), MCP HTTP boundary (4)
- `jest.config.js` — `testPathIgnorePatterns: ['/tests/smoke/']` so `npm test` skips smokes
- `package.json` — `npm run smoke` script

`npm test` → 198/198 in ~22s. `npm run smoke` → 7/7 in ~1.4s.

### 3. TOGT department population (PR DamianOost/george-brain#5)

Branch: `codex/togt-department-population` — 1 commit, 7 files.

- Promoted `business/togt/` from skeleton to active. Mirrors the Finance/Marketing pattern, slimmed for solo-operator (Damian-only).
- New: `AGENTS.md` with 23-row action table covering L0–L4, `MEMORY.md` (stable facts), `INBOX.md` (5 Open items seeded), `systems/source-map.md`
- Expanded: `IDENTITY.md` + `SCOPE.md` from one-line titles to full department contracts
- Root `AGENTS.md` routing table: added TOGT row pointing at the department file

### 4. Production deployment spec (PR DamianOost/Togt#3)

Branch: `docs/production-deployment-spec` — 2 commits.

- `docs/superpowers/specs/2026-05-27-production-deployment-design.md` (~290 lines)
- Vendor stack: Fly.io (compute, lhr, free tier) + Neon (Postgres, free tier with 7-day PITR built in) + HC.io (monitoring, free tier)
- 9 phases, each independently reversible
- Custom domain `togt.co.za` explicitly deferred — first deploy on `togt-api.fly.dev`, custom domain added later non-disruptively
- Fresh-start data approach (no migration from Mac dev DB)
- Total fixed cost at POC scale: **R0**

### 5. Production deployment plan (PR DamianOost/Togt#4, stacked on #3)

Branch: `docs/production-deployment-plan` — 1 commit.

- `docs/superpowers/plans/2026-05-27-production-deployment-plan.md` (~1000 lines)
- 16 step-by-step tasks with exact commands, expected outputs, branch names
- 6 L3 gates explicitly called out (vendor signups, secret minting, Fly secrets set, first deploy, prod identity bootstrap, schema migration)
- Recommended execution mode: inline (NOT subagent dispatch) — most steps need Damian-explicit go-ahead

### 6. Plan execution in flight — Tasks 1-4 partial (`feat/fly-production-deploy`)

Branch: `feat/fly-production-deploy` off `main` — 3 commits (NOT pushed):

```
66c5834 feat(deploy): fly.toml — lhr, shared-cpu-1x, auto-stop, /health check
d5513bd feat(deploy): .dockerignore — exclude tests/docs/.env from image
9979616 feat(deploy): Dockerfile for Fly.io production build
```

Express PORT handling verified: `backend/src/config/env.js:30` already has `port: process.env.PORT || 3000`. Fly will inject `PORT=8080` per `fly.toml`. No app code change needed.

---

## Where execution paused

Mid-**Plan Task 1** (vendor signups). State per vendor:

| Vendor | Status | Blocker |
|---|---|---|
| Fly.io | Account created (no card added yet). `flyctl` v0.4.56 installed on Mac mini at `/Users/georgeoosthuyzen/.fly/bin/flyctl`. **`flyctl auth login` failed twice** — once "Login expired" (URL window passed), once "not found" (server-side device-code timeout) | Pick CLI flow retry OR personal access token route |
| Neon | Account created. Project being created (TOGT name, Postgres 17, Frankfurt region, Neon Auth OFF). Default `neondb` database is fine; no need to rename | Damian to copy pooled connection string into 1Password under `TOGT production Neon DATABASE_URL — 2026-05-27` |
| HC.io | **Deferred to follow-up** per Damian's call this session — not done now | None; will revisit after first deploy lands |

**Plan Task 5 (generate the 4 secrets) is queued but paused** — it requires Damian present to immediately paste them into 1Password.

---

## Open PRs across both repos (in review order)

| Repo | PR | Branch | What |
|---|---|---|---|
| DamianOost/Togt | **#1** | `feat/audit-log-and-smokes` | Audit log + warm-ups |
| DamianOost/Togt | **#2** | `feat/smoke-tests` (stacked on #1) | Smoke harness + 7 smokes |
| DamianOost/Togt | **#3** | `docs/production-deployment-spec` | Production deploy spec |
| DamianOost/Togt | **#4** | `docs/production-deployment-plan` (stacked on #3) | Production deploy plan |
| DamianOost/george-brain | **#5** | `codex/togt-department-population` | TOGT dept promoted from skeleton |
| DamianOost/Togt | (not PR'd yet) | `feat/fly-production-deploy` | Dockerfile + .dockerignore + fly.toml (3 commits; will be PR'd as part of Plan Task 15 once deploy completes) |

---

## Where to start the next session

The blocker for forward motion is **getting flyctl authenticated on the Mac mini**. Once that lands:

1. **Damian generates a Fly personal access token** at `https://fly.io/user/personal_access_tokens` (more reliable than the device-grant CLI flow that timed out twice). Saves it to 1Password under `TOGT production Fly access token — 2026-05-27`. Sets it on the Mac via `flyctl auth token <paste>`. Verify: `~/.fly/bin/flyctl auth whoami` should print his email.

2. **Damian saves the Neon pooled connection string** to 1Password under `TOGT production Neon DATABASE_URL — 2026-05-27`.

3. **Resume from Plan Task 5** (generate 4 prod secrets — interactive with Damian to save to 1Password).

4. Then linear through Tasks 6 → 7 → 8 → 9 → 11 → 12 → 13 → 14 → 15 (per plan).

   - Task 10 (HC.io) is paused per Damian's call this session. It can land as a follow-up after the deploy.

5. **First MCP call to production** from Brave on `https://togt-api.fly.dev/mcp` is the "this is real" moment. The plan's Cutover Criteria § lists the 7 things that must pass for "deployed" to be true.

Recommended posture for the next session: load this handoff, load the plan, ask Damian to confirm Fly token + Neon URL are in 1Password, then resume from Task 5.

---

## Operating notes

- **SSH to Mac:** `ssh georgeoosthuyzen@100.65.206.24` (Tailscale-direct IP — magic-DNS `george` was broken this session because Damian was on phone hotspot bypassing an ISP DNS hijack of `controlplane.tailscale.com`). Direct IP always works.
- **Mac repo:** `~/.openclaw/workspace/Togt` on `feat/fly-production-deploy` (3 commits ahead of main; not pushed).
- **Mac stash:** `stash@{0}: On feat/smoke-tests: smoke-pause-2026-05-27` (created when switching branches for the spec write; harmless, drop or apply later as appropriate).
- **flyctl path:** `/Users/georgeoosthuyzen/.fly/bin/flyctl` (NOT on default PATH — call it fully qualified).
- **Backend dev/staging:** Mac launchd `com.togt.backend` port 3002 unchanged. Continues to serve dev traffic regardless of production state.
- **Working dev API key:** `togt_live_REVOKED_20260530` (mcp:full + admin:full) — live in the Mac's `togt` DB. Useful for local MCP testing while production is in flight.
- **Tests baseline:** 198 unit + 7 smoke. Three cold runs of each must stay green before any merge.
- **Vendor accounts created this session:**
  - Fly.io — `damianoost@gmail.com`, no card on file
  - Neon — `damianoost@gmail.com`
  - HC.io — deferred, not created

---

## Open decisions still in flight

1. **Fly auth path:** device-grant CLI retry OR personal access token. Damian to pick.
2. **Card on Fly:** still not added. Fly may prompt at `flyctl launch` time (Plan Task 6). If it does, decision is "add card with low-balance virtual card OR pause and reconsider".
3. **HC.io timing:** will be added as a follow-up once the deploy is live. Plan Task 10 is the recipe.
4. **Custom domain `togt.co.za`:** Damian to register "at some stage". Plan's "Future — Custom Domain Cutover" section in the spec documents the non-disruptive add-on path.
5. **Mobile production build:** Plan Task 12 hardcodes `https://togt-api.fly.dev`. Env-driven config (Option B in Task 12) is a future cleanup.

---

## Last words

The technical foundation is excellent. Three PRs of agent-native + observability work shipped before production deploy is critical-path. Once `togt-api.fly.dev` resolves and serves the MCP server, **TOGT becomes the first South African gig marketplace any AI agent can book a labourer through end-to-end**. The Path B bet stops being theoretical the moment that first external curl returns 200.

Pick up at branch `feat/fly-production-deploy` HEAD `66c5834`. Resume from Plan Task 5 once Fly auth + Neon URL are in 1Password.

Good luck.
