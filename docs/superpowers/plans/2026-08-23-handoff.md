# TOGT handoff — 2026-08-23

**Status:** Documentation baseline PR pending; dependent deployment-convergence candidate in progress

**Owner:** Damian Oosthuyzen

**Branch:** `codex/togt/current-state-plan-2026-08-23`

**Base:** `origin/main` at `389c81d`

**Evidence commit:** `d8dfea9`

**Draft PR:** `https://github.com/DamianOost/Togt/pull/7`

**Windows repository:** `C:\Users\PadelZone\Documents\GitHub\Togt`

**Task worktree:** `C:\Users\PadelZone\Documents\GitHub\_worktrees\Togt-current-state-plan-2026-08-23`

## 1. Why this handoff exists

TOGT had useful code and strong point-in-time documents, but no single current project baseline. The original project brief still described already-built features as missing, KYC setup still described the retired Smile ID path, deployment plans were stacked on older branches, and the Mac runtime no longer represented current `main`.

This handoff gives the new Windows TOGT project a safe starting point.

## 2. Project setup

Completed:

- cloned `https://github.com/DamianOost/Togt.git` to the Windows repository path above;
- verified a clean canonical checkout on `main...origin/main`;
- created the isolated task worktree/branch above from current `origin/main`;
- added a root `AGENTS.md` for future Codex project discovery.

Still required in the Codex UI:

1. Add `C:\Users\PadelZone\Documents\GitHub\Togt` as a local project.
2. Name it `TOGT`.
3. Make the repository folder primary.
4. Start future TOGT tasks from that project, using Codex worktrees for edits.

The app did not auto-register the cloned folder, and no available project-management tool could create the saved sidebar entry programmatically.

## 3. Verified Git state

At audit time:

- `origin/main`: `389c81d` (`Merge pull request #5 from DamianOost/feat/customer-data-safety`)
- PR 1 audit log: merged
- PR 2 smoke harness: merged
- PR 3 deployment design: open, based on older history
- PR 4 deployment implementation plan: open and stacked on PR 3
- PR 5 customer-data safety: merged
- PR 6 credential-reference redaction: merged
- `origin/feat/fly-production-deploy`: 21 commits behind and 5 commits ahead of `origin/main`
- old deployment branch contains Dockerfile, `.dockerignore`, Fly configuration, and a May handoff; it is source material, not a safe deployment candidate

Do not deploy, fast-forward, or resume secrets/vendor work from the old deployment branch.

## 4. Verified runtime state

Mac mini:

- repository: `/Users/georgeoosthuyzen/.openclaw/workspace/Togt`
- branch: `feat/fly-production-deploy`
- HEAD: `0564153`
- working tree: clean and synchronized with `origin/feat/fly-production-deploy`
- `http://127.0.0.1:3002/health`: healthy
- `http://127.0.0.1:3002/health/deep`: process, database, and dispatcher healthy

Public preview:

- `https://togt-api.fly.dev/health` did not resolve from Windows.
- No public production deployment was proven.

The Mac service is a usable development runtime, but it is behind current `main` and does not prove the merged privacy hardening is deployed.

## 5. Current implementation truth

Strong, implemented foundations:

- full account/session/password-reset path;
- labourer profiles/services/discovery/availability/location/ratings/earnings;
- direct booking and automatic match dispatch;
- booking states, scope changes, chat, safety, push notifications, and live tracking;
- Peach customer checkout/webhook code and cash fallback;
- VerifyNow integration and raw-ID minimization;
- RFC 9457, idempotency, API keys, MCP, OpenAPI, agent discovery, webhooks, and audit logging;
- migration 016 privacy hardening and state-based data reveal;
- 218 unit/integration plus 7 smoke declarations by static inventory.

Unproven or blocking:

- current `main` has not received a fresh three-cold-run unit/smoke verification in this audit;
- no public endpoint;
- old runtime branch is behind `main`;
- KYC vendor failure can still produce structural-only `verified` status;
- selfie enrolment is a POC/manual-review stub;
- no labourer payout;
- payment/refund/dispute/reconciliation not production-proven;
- mobile base URL is duplicated and defaults to a private-LAN HTTP address in `app.json`;
- OpenAPI omits multiple mounted route trees;
- agents manifest omits `audit_log_query`;
- legal/POPIA operator, retention, incident-contact, and publication gates remain open;
- no production mobile build or closed-pilot operating evidence.

## 6. Documents created or corrected on this branch

- `AGENTS.md` — project-local safety, workflow, context, tests, and deployment contract.
- `CONTRIBUTING.md` — human-facing entrypoint to the non-negotiable contribution workflow.
- `docs/runbooks/git-worktree-promotion.md` — complete task isolation, validation, PR, landing, cleanup, and deployment-separation procedure.
- `.github/pull_request_template.md` — mandatory evidence and Git/GitHub hygiene checklist for every PR.
- `.gitignore` and `.gitattributes` — local-secret/artifact exclusions and cross-platform LF normalization.
- `CLAUDE.md` — rewritten current project brief.
- `KYC_SETUP.md` — replaced retired Smile ID guidance with current VerifyNow/POPIA posture and explicit launch gates.
- `docs/superpowers/specs/2026-08-23-current-state-product-system-spec.md` — consolidated product/system truth and go/no-go criteria.
- `docs/superpowers/plans/2026-08-23-recovery-to-pilot-plan.md` — ordered path from Git convergence through synthetic preview to invited human pilot.
- `docs/superpowers/plans/2026-08-23-handoff.md` — this handoff.

No backend, mobile, database, runtime, vendor, credential, or production state was changed.

## 7. Recommended milestone

Build a **public technical preview using synthetic data** before attempting a real-user pilot.

Recommended proposed pilot boundary for Damian to approve:

- Durban/Umhlanga;
- plumbing first, at most two additional trades after stability;
- small manually invited supply and customer cohorts;
- one operator-owned agent integration first;
- no real-user KYC or money until the separate L3/legal/payment/payout gates are closed.

## 8. Decisions needed from Damian

1. Approve synthetic public preview as the next milestone.
2. Approve or change the Durban/Umhlanga, plumbing-first future pilot boundary.
3. Approve fail-closed/pending-review KYC behavior on VerifyNow failure.
4. Decide whether the selfie step should become real verification, explicit manual review, or be removed for the pilot.
5. Decide whether cash remains a supported live payment path.
6. Choose the labourer payout decision path/provider before a real-money pilot.
7. Confirm whether open PRs 3 and 4 should be superseded after a current deployment plan is ready.

## 9. Exact next task

Start a new task in the TOGT project titled **“Converge TOGT deployment work onto current main.”**

Scope:

1. read `AGENTS.md`, `CONTRIBUTING.md`, `docs/runbooks/git-worktree-promotion.md`, the 2026-08-23 spec, plan, and handoff;
2. fetch `origin/main`, PR 3, PR 4, and `origin/feat/fly-production-deploy`;
3. write a keep/replace matrix for the May deployment files and decisions;
4. create a fresh worktree/branch from current `origin/main`;
5. port only reviewed deployment configuration;
6. update secret/migration/mobile/health/rollback steps for migration 016 and current code;
7. build/test locally with synthetic configuration;
8. stop before vendor, secret, production database, DNS, or deploy writes.

## 10. Promotion checklist for this documentation branch

- complete documentation diff inspected;
- `git diff --check` passed;
- referenced repository paths confirmed;
- added-line scan found no credentials, raw IDs, customer data, or private vendor details;
- repository-local Git hygiene runbook and PR checklist added so future tasks do not depend on cross-repository memory;
- explicit documentation paths committed in `d8dfea9`;
- branch pushed and draft documentation PR 7 opened;
- Damian reviews before merge;
- after merge, register/open the TOGT local project and use this handoff for the first dedicated task.

## 11. Deployment convergence continuation

The exact next task above began on 2026-08-23 without changing PR #7 or `main`.

| Field | Current evidence |
|---|---|
| Repository | `DamianOost/Togt` |
| Owner | Current Codex deployment-convergence task; Damian remains release authority |
| Task branch | `codex/deployment/current-main-convergence-2026-08-23` |
| Task worktree | External isolated worktree; exact local path retained in the task report, not duplicated in repository history |
| Base | Fresh `origin/main` at `389c81d` |
| Planning dependency | PR #7 / `7aa54d1`; merged into the task branch only, not into `main` |
| Scope | May deployment matrix, replacement container/Fly config, startup readiness, current synthetic-preview runbook, validation, Draft PR |
| Non-goals | Merge, deploy, DNS/vendor/secret/production DB/Mac changes, real KYC/data/money, production API keys, or closing PRs #3/#4/#7 |

Reverified state:

- `origin/main`, the planning branch, and the old deployment branch have not moved from the earlier handoff commits;
- PR #7 remains Open, Draft, cleanly mergeable, and exactly 11 files;
- PR #3 remains Open and cleanly mergeable;
- PR #4 remains Open but is currently dirty/not mergeable against its stacked base;
- `origin/feat/fly-production-deploy` remains 21 behind and 5 ahead of `origin/main`;
- `https://togt-api.fly.dev` still does not resolve;
- the saved `main` checkout and planning worktree remain clean and preserved.

Prepared on the task branch:

- `docs/deployment/2026-08-23-may-convergence-matrix.md`;
- `backend/Dockerfile`, `backend/.dockerignore`, and `backend/fly.toml` rebuilt from current source truth;
- `docs/runbooks/synthetic-preview-deployment.md` with current migration, secret-name, mobile, health, smoke, rollback, and deployed-commit gates;
- immediate startup ticks so `/health/deep` can become ready without waiting an hour;
- Node 24/npm 11 engine declaration and a pinned Node 24 container base.

Validation evidence:

| Check | Result |
|---|---|
| Backend install | `npm ci` passed under Node 24.18.0/npm 11.16.0 in 46s |
| Migration replay | Migrations 001–016 passed twice in 2.86s and 0.53s on local PostgreSQL 17.11 `togt_test` |
| Schema | 20 public tables; five migration-016 fields; zero user/KYC/booking/payment rows immediately after schema creation |
| Focused readiness | 2 suites, 17 tests passed |
| Full backend | Initial run: 30 suites/216 tests passed and two pre-existing Windows `/tmp` assertions failed; after `os.tmpdir()` fix: 31 suites, 218 tests passed in 120.463s Jest time |
| Smoke | 3 suites, 7 tests passed in 13.654s Jest time |
| Dockerfile lint | Passed after making the Node tag/digest explicit |
| Public hostname | Still does not resolve |

Known blockers/limitations:

- no Docker-compatible runtime is installed on this Windows host, so image build, non-root filesystem inspection, Linux signal handling, and container-start evidence remain open;
- `npm audit --omit=dev` reports 27 production advisories: 1 critical, 16 high, and 10 moderate; dependency remediation is a separate prerequisite before preview promotion;
- tests emit a pre-existing `pg` deprecation warning about overlapping `client.query()` calls and Jest still requires `forceExit`;
- this is one green full/smoke run, not the later three-cold-run release-candidate gate;
- mobile endpoint unification, preview access control, KYC trust-state, agent contract, and payment integrity remain separate prerequisite branches.

The portable PostgreSQL test server was stopped after validation. Draft PR, commit, push, final diff, and sensitive-data evidence are not complete until the final task report updates this section. No current implementation unique to this task may be merged or deployed without the normal review and approval sequence.
